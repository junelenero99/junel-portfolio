// Theme toggle is intentionally removed — this portfolio uses a fixed dark cyberpunk theme.
// The file is kept to avoid breaking any imports that may reference it.
export function ThemeToggle() {
  return null
}
